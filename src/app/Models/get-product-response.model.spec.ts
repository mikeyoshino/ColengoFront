import { GetProductResponse } from './get-product-response.model';

describe('GetProductResponse', () => {
  it('should create an instance', () => {
    expect(new GetProductResponse()).toBeTruthy();
  });
});
